/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
};


/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/zaid',
  trailingSlash: true, // optional but helps with static exports sometimes
};


module.exports = nextConfig;